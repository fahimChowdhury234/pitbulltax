import EmailClientPicker from "./EmailClientPicker";

export default function QuestionnaireClientPicker() {
  // reuse the same list look, just route to questionnaire later
  return <EmailClientPicker kind="template" />;
}
